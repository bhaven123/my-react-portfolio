import classes from "./About.module.css";

const About = () => {
  return (
    <div id="about" className={classes.position}>
      <h1 className={classes.h1}>This is the About page.</h1>
    </div>
  );
};

export default About;
